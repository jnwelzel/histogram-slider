import React from "react";

const Background = ({ style }) => {
  return (
    <div
      style={{
        ...style,
        backgroundColor: "#B5BEC5",
        height: 2,
      }}
    />
  );
};

export default Background;
